{these are the function def's for rand and xr** both erl's need to be 
 included for this to work -- xr80 for 8080 and xr86 for 8088/6 }

external function xrand(seed:integer) :integer;
external procedure randomize(seed : integer);
external function random(range :integer) : integer;

